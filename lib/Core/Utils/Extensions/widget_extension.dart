import 'package:flutter/material.dart';import 'package:zan/Config/app_config.dart';extension ContextExtension on Widget {  Widget get stylePagesInMain => SafeArea(        child: Padding(          padding:AppConfig.customPadding,          child: this,        ),      );  Widget get stylePages1 => SafeArea(        child: Center(          child: this,        ),      );  Widget get stylePages2 => SafeArea(        child: this,      );}