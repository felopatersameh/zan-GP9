import 'package:flutter/material.dart';import 'package:flutter_screenutil/flutter_screenutil.dart';import '../../../../Config/app_config.dart';import 'custom_search_text_field.dart';class SearchViewBody extends StatefulWidget {  const SearchViewBody({super.key});  @override  State<SearchViewBody> createState() => _SearchViewBodyState();}class _SearchViewBodyState extends State<SearchViewBody> {  FocusNode focusNode = FocusNode();  TextEditingController searchController =      TextEditingController(text: ""); // Internal controller (if needed)  @override  Widget build(BuildContext context) {    return Scaffold(      body: SafeArea(        child: Padding(          padding: AppConfig.customPadding,          child: Column(            children: [              CustomSearchTextField(),              32.horizontalSpace,              // Text(              //   'Search...',              //   style:              //       AppTextStyles.h7Medium.copyWith(color: AppColors.grayscale60),              // ),              // const SizedBox(              //   height: 16,              // ),              // if (state is SearchSuccess)              //   Expanded(              //     child: ListViewBooksVertical(              //       items: state.books,              //     ),              //   ),              // if (state is SearchFailure)              //   Expanded(              //       child: CustomMassage(              //     massage: state.error,              //   )),              // if (state is SearchLoading) const Expanded(child: CustomLoading())            ],          ),        ),      ),    );  }}